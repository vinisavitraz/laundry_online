export enum OrderStatusEnum {
    CREATED = 'created',
    OPEN = 'open',
    REJECTED = 'rejected',
    CANCELED = 'canceled',
    COLLECTED = 'collected',
    WAITING_PAYMENT = 'waiting_payment',
    PAID = 'paid',
    FINISHED = 'finished',
}